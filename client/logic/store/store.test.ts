import store from "./store";
import { defaultLocation } from "./location";
import { Coordinate } from "../types";

describe("store/strava", () => {
  it("we get an error if the browser is not supported", (done) => {
    expect(true).toBe(true);
    done();
  });
});
